{
  "name": "@ortense/mediator",
  "version": "0.1.0",
  "description": "A minimal implementation for a dependency-free event broker with internal context for front-end",
  "main": "dist/index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --loader tsx --test **/*.spec.ts",
    "test:watch": "node --loader tsx --test --watch **/*.spec.ts",
    "test:coverage": "node --loader tsx --experimental-test-coverage --test **/*.spec.ts",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.5.1",
    "tsx": "^3.12.7",
    "typescript": "^5.1.6"
  },
  "keywords": [
    "mediator",
    "mediator design pattern",
    "observer",
    "pubsub",
    "pubsub",
    "events",
    "emitter",
    "eventemitter",
    "dependency-free"
  ]
}
